# Секреты — никогда не коммитим
**/.env

# репозиторий openclaw (клонируется отдельно, в основной репо не коммитим)
openclaw-devops/openclaw/

# репозиторий ouroboros (клонируется отдельно, в основной репо не коммитим)
ouroboros/ouroboros-desktop/
ouroboros/state

# Рабочие директории агентов
**/workspace/*
!**/workspace/.gitkeep

# Локальные данные
**/data/

# Дампы Postgres (бэкапы pg_dump — данные БД, не коммитим)
*.dump
*.sql

# Артефакты IDE и ОС
.vscode/
.idea/
*.swp
.DS_Store

# Кеш зависимостей внутри src/ (если MCP на Node/Python/Rust)
