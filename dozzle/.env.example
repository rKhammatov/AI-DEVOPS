# Скопируй в .env и заполни реальными значениями.
# .env не коммитится в git (см. .gitignore).

# Логин и пароль для веб-интерфейса Dozzle.
# Оставь пустыми только если VPS закрыт firewall-ом и доступ только через туннель.
DOZZLE_USERNAME=admin
DOZZLE_PASSWORD=замени_на_надёжный_пароль
